import 'package:flutter/material.dart';

class TestInstructionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Test Instructions'),
      ),
      body: Center(
        child: Text('Content for Test Instructions'),
      ),
    );
  }
}
