import 'package:flutter/material.dart';

class LaneLayoutPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lane Layout Instructions'),
      ),
      body: Center(
        child: Text('Content for Lane Layout'),
      ),
    );
  }
}
