import 'package:flutter/material.dart';

class SPTInstructions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('SPT Instructions'),
      ),
      body: Center(
        child: Text('Content for SPT Instructions'),
      ),
    );
  }
}
